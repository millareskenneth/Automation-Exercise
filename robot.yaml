# Robot Framework execution configuration
tasks:
  - tests/

variables:
  BASE_URL: "https://www.saucedemo.com"
  BROWSER: "chrome"
  HEADLESS: "false"
  SELENIUM_SPEED: "0.5s"


output_dir: results

robot:
  output: output.xml
  log: log.html
  report: report.html
